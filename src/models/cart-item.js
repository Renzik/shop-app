class CartItem {
  constructor(quantity, price, title, sum, images) {
    this.quantity = quantity;
    this.price = price;
    this.title = title;
    this.sum = sum;
    this.images = images;
  }
}

export default CartItem;
