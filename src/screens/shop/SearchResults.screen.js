import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const SearchResults = ({ navigation, items }) => {
  return (
    <View>
      <Text>SEARCH RESULTSSSS</Text>;
    </View>
  );
};

export default SearchResults;

const styles = StyleSheet.create({});
